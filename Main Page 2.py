# Create buttons for Existing User and New User
existing_user_btn = tk.Button(root, text="Existing User", command=on_button_click)
new_user_btn = tk.Button(root, text="New User", command=on_button_click)
existing_user_btn.pack()
new_user_btn.pack()
